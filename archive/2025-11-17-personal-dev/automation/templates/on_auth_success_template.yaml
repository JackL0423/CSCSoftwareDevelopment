## Template: On Auth Success → Custom Action
# Same content structure as generic action file; trigger wiring references this action.
# Placeholders:
#   ${ACTION_NAME}
#   ${CUSTOM_ACTION_KEY}
#   ${NON_BLOCKING}

kind: action
version: 1
action:
  customAction:
    customActionIdentifier:
      name: ${ACTION_NAME}
      key: ${CUSTOM_ACTION_KEY}
    argumentValues: {}
  nonBlocking: ${NON_BLOCKING}
