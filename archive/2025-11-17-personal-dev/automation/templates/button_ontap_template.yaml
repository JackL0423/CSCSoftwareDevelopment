## Template: Button OnTap → Custom Action with parameters
# Placeholders:
#   ${ACTION_NAME}
#   ${CUSTOM_ACTION_KEY}
#   ${NON_BLOCKING}
#   ${PARAMS_JSON}  JSON object of argumentValues (serialized)

kind: action
version: 1
action:
  customAction:
    customActionIdentifier:
      name: ${ACTION_NAME}
      key: ${CUSTOM_ACTION_KEY}
    argumentValues: ${PARAMS_JSON}
  nonBlocking: ${NON_BLOCKING}
