# GlobalFlavors - PRODUCTION Environment
#
# This file references Google Secret Manager - NO ACTUAL SECRETS STORED HERE
# Secrets are fetched at runtime using the secret helper script
#
# ⚠️ CRITICAL: This file should NEVER be committed to git
# ⚠️ Production secrets must be stored ONLY in Google Secret Manager
#
# Usage: export NODE_ENV=production && source scripts/load-secrets.sh && npm start

# =============================================================================
# ENVIRONMENT
# =============================================================================
NODE_ENV=production
ENVIRONMENT=production

# =============================================================================
# GOOGLE CLOUD PROJECT (for Secret Manager)
# =============================================================================
GCP_PROJECT_ID=csc305project-475802
GCP_PROJECT_NUMBER=815086348049

# =============================================================================
# SECRET MANAGER REFERENCES - PRODUCTION VERSIONS
# =============================================================================
# Production secrets use "_PROD" suffix for maximum isolation
# NEVER use dev/testing secrets in production!

# FlutterFlow API (production project)
FLUTTERFLOW_API_TOKEN_SECRET=FLUTTERFLOW_API_TOKEN_PROD
FLUTTERFLOW_PROJECT_ID_SECRET=FLUTTERFLOW_PROJECT_ID_PROD

# Figma API (production design file)
FIGMA_API_KEY_SECRET=FIGMA_API_KEY_PROD
FIGMA_FILE_ID_SECRET=FIGMA_FILE_ID_PROD

# Firebase (production project - CRITICAL)
FIREBASE_PROJECT_ID_SECRET=FIREBASE_PROJECT_ID_PROD
FIREBASE_WEB_API_KEY_SECRET=FIREBASE_WEB_API_KEY_PROD
FIREBASE_SERVICE_ACCOUNT_SECRET=FIREBASE_SERVICE_ACCOUNT_JSON_PROD

# Third-party integrations (production API keys)
INSTACART_API_KEY_SECRET=INSTACART_API_KEY_PROD
INSTACART_CLIENT_ID_SECRET=INSTACART_CLIENT_ID_PROD
WALMART_API_KEY_SECRET=WALMART_API_KEY_PROD
WALMART_CLIENT_ID_SECRET=WALMART_CLIENT_ID_PROD
AMAZON_AFFILIATE_ID_SECRET=AMAZON_AFFILIATE_ID_PROD

# Database (production instance)
DATABASE_URL_SECRET=DATABASE_URL_PROD

# Encryption keys (for sensitive data)
ENCRYPTION_KEY_SECRET=ENCRYPTION_KEY_PROD
JWT_SECRET_SECRET=JWT_SECRET_PROD

# =============================================================================
# NON-SENSITIVE CONFIGURATION
# =============================================================================
# API Base URLs (production endpoints)
FLUTTERFLOW_API_BASE_URL=https://api.flutterflow.io/v2
FIGMA_API_BASE_URL=https://api.figma.com/v1

# Production settings - OPTIMIZED FOR PERFORMANCE & SECURITY
DEBUG=false
LOG_LEVEL=error
API_RATE_LIMIT=1000
API_TIMEOUT=30000
API_RETRY_ATTEMPTS=3

# Security settings
ENABLE_CORS=true
ALLOWED_ORIGINS=https://globalflavors.app,https://www.globalflavors.app
ENABLE_RATE_LIMITING=true
MAX_REQUEST_SIZE=10mb

# Performance settings
ENABLE_CACHING=true
CACHE_TTL=3600
ENABLE_COMPRESSION=true

# Monitoring & Analytics
ENABLE_MONITORING=true
ENABLE_ERROR_TRACKING=true
SENTRY_DSN_SECRET=SENTRY_DSN_PROD

# Team emails (for alerts)
TEAM_EMAIL=uricsc305@gmail.com
ALERT_EMAIL=uricsc305@gmail.com

# Firebase settings (production mode)
USE_FIREBASE_EMULATOR=false
ENABLE_FIRESTORE_OFFLINE=true

# Deployment metadata
DEPLOY_VERSION=${DEPLOY_VERSION:-unknown}
DEPLOY_TIMESTAMP=${DEPLOY_TIMESTAMP:-unknown}
GIT_COMMIT_SHA=${GIT_COMMIT_SHA:-unknown}
